<%= form_with(model: create_task) do |form| %>
  <% if create_task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(create_task.errors.count, "error") %> prohibited this create_task from being saved:</h2>

      <ul>
        <% create_task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
